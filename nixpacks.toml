[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "if [ \"$RAILWAY_SERVICE_NAME\" = \"nursinghomesnearme_backend\" ] || [ \"$RAILWAY_SERVICE_NAME\" = \"nursinghomesnearme-backend\" ]; then npm run start:backend; else npm run start; fi"
